import { test, expect } from '@playwright/test'

test('popup shows buttons', async ({ page }) => {
  // Load the extension
  // This would require setting up the extension in Playwright
  // For now, placeholder
  expect(true).toBe(true)
})